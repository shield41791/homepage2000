Scrolling announcement bar: dark plum background, neon-green serif text, runs under the banner on every page.

```jsx
<Marquee text="☆＊。 어서오세요 ~ 새 글은 [N] 표시를 확인하세요 ~ ♡ 。＊☆" />
```

Respect `reduce motion` at the page level by pausing `animation-play-state` — the component itself always animates.
